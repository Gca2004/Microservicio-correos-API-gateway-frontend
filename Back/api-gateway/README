
Librerias usadas en la api gateway:

fastapi
uvicorn
httpx
python-dotonev


##Para iniciar el front hacer lo siguiente

#entrar a la siguiente ruta: ruta donde tengan el back/api-correos desde el cmd

#y poner el siguiente comando : python -m uvicorn main:app --port 8000 --reload